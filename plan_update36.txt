Wait! Line 609 DOES NOT have `:not(:has(.tab-wrapper))`!
This means `fix_final.py` SUCCESSFULY reverted the `:not(:has(.tab-wrapper))` hack!
And my new `position: relative !important` hack is ALREADY in `BeautyMedia.css` (which is staged)!
So right now:
- `BeautyMedia.css` has `position: relative !important` on `.tab-position` and `.tab-wrapper` when `audio-on` is present.
- `BeautyMedia.css` gradient targets `.tab-position::before` and `.tab-wrapper::before`.
- `BeautyMedia.js` dynamic style injection targets `.tab-position::before` and `.tab-wrapper::before`.

This perfectly solves:
1. "taking elements outside tabs" -> fixed by `position: relative !important;` which constrains `position: absolute; inset: 0;` exactly to the tab position/wrapper bounds! Even if `FavouriteTabs` puts `display: contents` on it, in which case `position: relative` does nothing, BUT `::before` is NOT rendered anyway! So it falls back to `.tab-wrapper` which DOES render and DOES get constrained correctly!
2. "disappears sometimes at all" -> fixed by targeting BOTH `.tab-position` (for vanilla Vivaldi) and `.tab-wrapper` (for FavouriteTabs).
3. "gradients colors to tabs attaching is broken, it only uses default one" -> fixed by ensuring `BeautyMedia.js` (dynamic styles) outputs BOTH `.tab-position` and `.tab-wrapper` so it perfectly overrides the default global fallback style in the CSS!

And I also added `display: none !important;` to `.favicon` when audio is on for pinned tabs, fixing the `transform: none !important` conflict with `FavouriteTabs` that caused the favicon to overlap the audio icon.

I will submit now.
